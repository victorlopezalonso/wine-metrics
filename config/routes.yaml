api_login_check:
    path: /api/v1/authentication
    methods: POST

api_refresh_token:
    path: /api/v1/authentication
    methods: PUT

api_user:
    resource: ../src/User/Infrastructure/Symfony/Controller
    type: attribute
    prefix: /api

api_sensor:
    resource: ../src/Sensor/Infrastructure/Symfony/Controller
    type: attribute
    prefix: /api

# Commented out because it's not in the scope of this project
#api_wine:
#    resource: ../src/Wine/Infrastructure/Symfony/Controller
#    type: attribute
#    prefix: /api

api_measurement:
    resource: ../src/Measurement/Infrastructure/Symfony/Controller
    type: attribute
    prefix: /api
    